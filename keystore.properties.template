# ═══════════════════════════════════════════════════════════════════════════
# 🔐 RELEASE SIGNING CONFIGURATION - NOMM (Nothing On My Mind)
# ═══════════════════════════════════════════════════════════════════════════
#
# Copy this file to 'keystore.properties' and fill in your values.
# NEVER commit keystore.properties to version control!
#
# To generate a new keystore, run:
#   keytool -genkey -v -keystore nomm-release.keystore -alias nomm -keyalg RSA -keysize 2048 -validity 10000
#
# ═══════════════════════════════════════════════════════════════════════════

# Path to your keystore file (relative to project root or absolute path)
storeFile=nomm-release.keystore

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD

# Key alias (the name you gave when creating the keystore)
keyAlias=nomm

# Key password
keyPassword=YOUR_KEY_PASSWORD
